server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # La directive  pour le routage Vue.js (SPA)
        try_files $uri $uri/ /index.html;
    }
}