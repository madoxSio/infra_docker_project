name: CI/CD Microservices

on:
  push:
    branches: [ main ]
    tags:
      - 'v*' # Se déclenche sur les tags comme v1.0, v2.1...
  pull_request:
    branches: [ main ]

jobs:
  # --- PARTIE CI : TESTS ET QUALITÉ ---
  tests-and-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install MongoDB dependencies
        run: |
          sudo apt-get update
          wget http://archive.ubuntu.com/ubuntu/pool/main/o/openssl/libssl1.1_1.1.1f-1ubuntu2_amd64.deb
          sudo dpkg -i libssl1.1_1.1.1f-1ubuntu2_amd64.deb
          # Installation des autres dépendances standard
          sudo apt-get install -y libcurl4 libsasl2-2 libsnappy1v5

      # Tests Frontend
      - name: Frontend Unit Tests
        run: |
          cd frontend
          npm install
          npm run test:unit

      # Tests Backend 
      - name: Auth Service Unit Tests
        run: |
          cd services/auth-service
          npm install
          npm test

      - name: product Service Unit Tests
        run: |
          cd services/product-service
          npm install
          npm test

      - name: order Service Unit Tests
        run: |
          cd services/order-service
          npm install
          npm test

      # Scan de sécurité avec Trivy
      - name: Security Scan (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          severity: 'CRITICAL,HIGH'

  # --- PARTIE CD : DÉPLOIEMENT (UNIQUEMENT SUR TAG) ---
  deploy-production:
    needs: tests-and-security
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/madox/infra_docker_project
            git fetch --all
            git checkout ${{ github.ref_name }}
            # On recrée les conteneurs avec les nouvelles images
            docker compose up -d --build